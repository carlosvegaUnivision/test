name: Add comment to pull request

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  build_test:
    uses: ./share.yml@main
    with:
      caller_id: 'QA input from pr.yml to share.yml'
